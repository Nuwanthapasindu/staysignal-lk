# API base for the shared axios client. Modules pass paths relative to this
# (`/notices`, `/auth/login`, …). Dev: `/api` → Vite proxy → backend :5000.
# Prod: the backend's public API base, e.g. https://api.staysignal.lk/api
VITE_API_URL=/api

# Optional display
VITE_APP_NAME=StaySignal LK
