# API base. Leave empty in dev — API calls go to /api/... on the same origin and
# the Vite proxy (vite.config.js) forwards them to the backend on :5000.
# In production set the backend's public origin, e.g. https://api.staysignal.lk
VITE_API_URL=

# Optional display
VITE_APP_NAME=StaySignal LK
